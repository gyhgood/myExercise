策略模式：
将某种行为抽象为一个接口，把这个接口的不同的实现。封装为一个算法组。
可以被轻易的扩充和改变。在调用时以接口的引用作为参数。这个引用就是所谓的策略
要用什么策略取决于用户使用运行时多态行为


课后练习：
有一个character（角色基类）和几个子类
一个weaponbehavior兵器攻击的接口和几个实现类（把这部分封装为算法组），weaponbehavior的引用作为策略

目的：
选择一个角色，可以随时更换任何一种兵器攻击